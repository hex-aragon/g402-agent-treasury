import test from "node:test";import assert from "node:assert/strict";
import { resourceHash,assertSettlementAllowed } from "../lib/domain.ts";
test("resource hash binds method and url",()=>{assert.notEqual(resourceHash("GET","https://a.test/x"),resourceHash("POST","https://a.test/x"))});
test("settlement fails closed",()=>{const old=process.env.G402_ENABLE_SETTLEMENT;delete process.env.G402_ENABLE_SETTLEMENT;assert.throws(()=>assertSettlementAllowed("gno:staging"),/settlement_disabled/);process.env.G402_ENABLE_SETTLEMENT=old});
test("mainnet needs independent lock",()=>{process.env.G402_ENABLE_SETTLEMENT="true";delete process.env.G402_ALLOW_MAINNET;assert.throws(()=>assertSettlementAllowed("gno:mainnet"),/mainnet_locked/);process.env.G402_ENABLE_SETTLEMENT="false"});
test("unknown Gno networks are locked unless explicitly authorized",()=>{const old={enabled:process.env.G402_ENABLE_SETTLEMENT,allow:process.env.G402_ALLOW_MAINNET,network:process.env.GNO_NETWORK_ID,chain:process.env.GNO_CHAIN_ID};try{process.env.G402_ENABLE_SETTLEMENT="true";delete process.env.G402_ALLOW_MAINNET;process.env.GNO_NETWORK_ID="gno:customnet";process.env.GNO_CHAIN_ID="customnet";assert.throws(()=>assertSettlementAllowed("gno:customnet","customnet"),/mainnet_locked/)}finally{for(const [key,value] of Object.entries({G402_ENABLE_SETTLEMENT:old.enabled,G402_ALLOW_MAINNET:old.allow,GNO_NETWORK_ID:old.network,GNO_CHAIN_ID:old.chain})){if(value===undefined)delete process.env[key];else process.env[key]=value}}});
