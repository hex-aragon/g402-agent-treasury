export type CosmosChain={caip2:string;chainId:string;rpc:string;bech32Prefix:string;testnet:boolean;denoms:string[];ibcChannels:string[]};
const defaults:CosmosChain[]=[{caip2:"cosmos:localnet-1",chainId:"localnet-1",rpc:"http://127.0.0.1:26657",bech32Prefix:"cosmos",testnet:true,denoms:["uatom"],ibcChannels:["channel-0"]},{caip2:"cosmos:provider",chainId:"provider",rpc:"",bech32Prefix:"cosmos",testnet:true,denoms:["uatom"],ibcChannels:[]}];
export function chainRegistry():CosmosChain[]{if(!process.env.COSMOS_CHAINS_JSON)return defaults;const parsed=JSON.parse(process.env.COSMOS_CHAINS_JSON) as CosmosChain[];for(const chain of parsed)if(chain.caip2!==`cosmos:${chain.chainId}`)throw new Error("invalid_caip2_chain");return parsed.filter(chain=>chain.testnet||process.env.COSMOS_ALLOW_MAINNET==="true")}
export function getChain(caip2:string){const chain=chainRegistry().find(x=>x.caip2===caip2);if(!chain)throw new Error("cosmos_chain_unsupported");return chain}
export function assertCosmosSettlement(chain:CosmosChain){if(process.env.COSMOS_ENABLE_SETTLEMENT!=="true")throw new Error("cosmos_settlement_disabled");if(!chain.testnet&&process.env.COSMOS_ALLOW_MAINNET!=="true")throw new Error("cosmos_mainnet_locked");if(!chain.rpc)throw new Error("cosmos_rpc_missing")}
