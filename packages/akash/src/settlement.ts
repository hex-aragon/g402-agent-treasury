import type {SettlementAdapter} from "../../x402-core/src/index.ts";
import {findPayment} from "../../../lib/store.ts";
export class G402SettlementAdapter implements SettlementAdapter{async verify(paymentId:string,quoteId:string,amount:string,agentId?:string){const payment=await findPayment(paymentId);if(!payment)return {valid:false,reason:"payment_not_found"};if(payment.status!=="settled")return {valid:false,reason:"payment_not_finalized"};if(payment.network==="gno:mainnet")return {valid:false,reason:"mainnet_locked"};if(payment.serviceQuoteId!==quoteId)return {valid:false,reason:"quote_payment_mismatch"};if(BigInt(payment.amount)<BigInt(amount))return {valid:false,reason:"underpayment"};if(agentId&&payment.agentId!==agentId)return {valid:false,reason:"agent_payment_mismatch"};return {valid:true,transaction:payment.txHash||undefined}}}
