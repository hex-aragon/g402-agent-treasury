import {CircuitBreaker} from "../../x402-core/src/index.ts";
import type {AkashProvider} from "./domain.ts";
export const akashCircuit=new CircuitBreaker(3,30_000);
export function configuredProviders():AkashProvider[]{if(process.env.AKASH_PROVIDERS_JSON){const value=JSON.parse(process.env.AKASH_PROVIDERS_JSON) as AkashProvider[];return value.filter(p=>p.enabled)}return [{id:"akashml",baseUrl:process.env.AKASHML_BASE_URL||"https://api.akashml.com/v1",region:"global",models:(process.env.AKASH_MODELS||"Meta-Llama-3.3-70B-Instruct,Qwen3-30B-A3B").split(","),priority:100,inputMicrousdPerMillion:"130000",outputMicrousdPerMillion:"400000",fixedMicrousd:"0",apiKeyEnv:"AKASHML_API_KEY",enabled:true}]}
export function selectProvider(model:string,region?:string,providers=configuredProviders()){const candidates=providers.filter(p=>p.models.includes(model)&&akashCircuit.available(p.id));if(!candidates.length)throw new Error("akash_provider_unavailable");return candidates.sort((a,b)=>{const regionA=region&&a.region===region?0:1,regionB=region&&b.region===region?0:1;return regionA-regionB||a.priority-b.priority||Number(BigInt(a.inputMicrousdPerMillion)-BigInt(b.inputMicrousdPerMillion))})[0]}
