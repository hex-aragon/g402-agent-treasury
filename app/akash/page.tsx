import {configuredProviders} from "@/packages/akash/src/routing";
export default function Akash(){const providers=configuredProviders(),models=[...new Set(providers.flatMap(p=>p.models))];return <><div className="pageHead"><div className="eyebrow">x402 compute market</div><h1>Akash AI & GPU Gateway</h1><p>OpenAI-compatible inference and bounded container leases, prepaid by autonomous agents.</p></div><div className="grid stats"><div className="card stat"><label>ROUTABLE PROVIDERS</label><strong>{providers.length}</strong></div><div className="card stat"><label>MODELS</label><strong>{models.length}</strong></div><div className="card stat"><label>SETTLEMENT</label><strong>g402</strong><small>staging</small></div><div className="card stat"><label>AKASH MAINNET</label><strong>LOCKED</strong></div></div><div className="grid twoCol"><div className="card feature"><h3>OpenAI-compatible inference</h3><p>POST chat completion requests. The first response is an exact x402 quote; the paid retry is routed by model, region, health and price.</p><pre className="code">POST /api/akash/v1/chat/completions{"\n"}GET /api/akash/v1/models</pre></div><div className="card feature"><h3>GPU & container leases</h3><p>Images, resource ceilings, runtime and GPU type are validated before a time-bounded deployment quote is issued.</p><pre className="code">POST /api/akash/v1/deployments</pre></div></div><div className="card feature" style={{marginTop:14}}><h3>Available models</h3>{models.map(model=><div className="statusLine" key={model}><span>{model}</span><b className="success">x402 ready</b></div>)}</div></>}
