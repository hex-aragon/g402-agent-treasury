import CosmosWallet from "./CosmosWallet";
import {chainRegistry} from "@/packages/cosmos/src/registry";
export default function Cosmos(){const chains=chainRegistry();return <><div className="pageHead"><div className="eyebrow">native Cosmos x402</div><h1>Cosmos Facilitator</h1><p>CAIP-2 chain registry, direct-sign verification, bank/IBC settlement and feegrant-aware agent policy.</p></div><div className="grid stats"><div className="card stat"><label>REGISTERED CHAINS</label><strong>{chains.length}</strong></div><div className="card stat"><label>SIGN MODE</label><strong>DIRECT</strong></div><div className="card stat"><label>TRANSFERS</label><strong>BANK + IBC</strong></div><div className="card stat"><label>MAINNET</label><strong>LOCKED</strong></div></div><div className="card" style={{marginBottom:14}}>{chains.map(chain=><div className="statusLine" key={chain.caip2}><span className="mono">{chain.caip2}</span><b>{chain.bech32Prefix} · {chain.testnet?"testnet":"mainnet"}</b></div>)}</div><CosmosWallet/></>}
