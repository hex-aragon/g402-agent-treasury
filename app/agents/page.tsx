import {listAgents} from "@/lib/store";
export const dynamic="force-dynamic";
export default async function Agents(){const agents=await listAgents();return <><div className="pageHead"><div className="eyebrow">Agent economy</div><h1>Agents & policies</h1><p>Registered non-custodial wallets with server-enforced spending controls.</p></div><div className="grid twoCol">{agents.length?agents.map(agent=><div className="card feature" key={agent.id}><div className="eyebrow">{agent.status}</div><h3>{agent.name}</h3><p className="mono">{agent.walletAddress}</p><div className="statusLine"><span>Agent ID</span><b className="mono">{agent.id.slice(0,8)}…</b></div></div>):<div className="card feature"><h3>No agents registered</h3><p>Create an agent through PUT /api/v1/agents, then attach a policy through PUT /api/v1/policies.</p></div>}</div></>}
